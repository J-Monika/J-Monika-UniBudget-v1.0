{
  "name": "unibudget",
  "version": "1.0.0",
  "description": "UniBudget — student budget tracker with GCash auto-detection",
  "private": true,
  "scripts": {
    "test": "node tests/cloud-sync-auth.test.js && node tests/notifications.test.js && node tests/gcash-dedup.test.js && node tests/peer-ledger.test.js && node tests/transaction-inspector.test.js && node tests/gamification.test.js",
    "sync": "cap sync android",
    "open": "cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/app": "^6.0.3",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "@capacitor/local-notifications": "^6.1.3",
    "@supabase/supabase-js": "^2.110.8"
  }
}
